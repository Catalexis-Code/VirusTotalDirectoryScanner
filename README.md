# VirusTotalDirectoryScanner
A background process that monitors a specific folder for files, and once they are detected, the files are submitted to the VirusTotal API for scanning, and then moved either into clean or compromised folders.
